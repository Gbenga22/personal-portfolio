# Portfolio site source

This folder mirrors the React + Vite portfolio application used by the live Replit artifact. The standalone showcase prototypes live in `../projects`.

The site is intentionally kept in the workspace's monorepo layout because it uses the shared `@workspace/api-client-react` package and scaffold conventions. Replace placeholder personal details in `src/App.tsx` before sharing publicly.
